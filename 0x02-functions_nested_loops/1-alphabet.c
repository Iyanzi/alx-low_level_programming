#include "main.h"

/**
 * print_alphabet - prints the alpha in lowercase
 *
 * Return: Always return 0.
 *
 */

void print_alphabet(void)
{
	char letter;
	for (lc = 'a'; lc<= 'z'; lc++)
	{
		_putchar(lc)
	}
	_putchar('\n');
	return(0);
}
