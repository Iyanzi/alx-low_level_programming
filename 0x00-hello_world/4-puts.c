#include <stdio.h>
/**
 *main- Serves as the entry point for the program
 *
 *Return: By default retuens to zero if no errors otherwise returns non-zero value
 */
int main(void)
{
	puts("\"Programming i like buildig a mulitilingual puzzle");
	return (0);
}
